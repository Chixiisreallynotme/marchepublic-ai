# Ralph Progress Log
Started: 2026-08-24 15:30:00
---

## Codebase Patterns

- Framework: Next.js 15 App Router + TypeScript (strict), React 19
- Styling: Tailwind CSS with `cn()` helper (clsx + tailwind-merge); design tokens in globals.css + tailwind.config.ts
- Database: Prisma ORM over SQLite (`prisma/schema.prisma`, singleton in lib/prisma.ts); `npm run db:push` applies schema
- Validation: Zod schemas colocated under lib/schemas/; ActionResult<T> pattern { success, data | error, issues }
- Server Actions in lib/actions/*("use server"); REST routes under app/api/* for client fetch
- Testing: Vitest + @testing-library/react + jsdom in __tests__/; prisma mocked via vitest.setup.ts
- Mono-tenant V1: single organization via lib/org.ts getActiveOrganization()
- Sirene source: ETALAB recherche-entreprises.api.gouv.fr (no token); mock only with SIRENE_MOCK=1
- CERFA PDFs rendered server-side with pdf-lib at GET /api/cerfa/[id]/pdf

---
## US-007..US-016 — Production features (2026-08-24)
- Status: DONE (10 stories)
- lib/org.ts getActiveOrganization() kills the org-1 hardcode; memory page resolves real seeded memory
- Sirene: ETALAB recherche-entreprises API primary (verified live with SIREN 552100554), INSEE optional, mock only behind SIRENE_MOCK=1
- /tenders/new full DCE form + POST /api/upload (PDF/ZIP ≤25MB → public/uploads) + Tender.dceUrl column
- CERFA: pdf-lib renderer DC1/DC2, GET /api/cerfa/[id]/pdf streams real PDFs and backfills fileUrl, legacy seed payload normalized via normalizeCerfaPayload
- Standalone pages: /sirene (search+cache list), /cerfa hub grouped by tender, /memories weighted progress cards
- /simulation: rule-based Σ(weight×completion) score /100 +/20, AI review panel via LLM_API_KEY env (OpenAI-compatible), setup card when key missing
- Landing: live DB stats replace fictional ×3/+250/100%; bento gapless grid; Instrument Serif display font; Reveal IO animations with reduced-motion support
- Legal pages: mentions-legales, confidentialite, cgu, accessibilite
- Verification: tsc 0 errors | 214/214 vitest | next build 0 errors | all 15 routes HTTP 200 | Playwright verified memory/simulation/new pages
---
## US-017..US-019 — GAFAM UI polish (2026-08-24)
- Status: DONE
- Navbar active-link states via usePathname (desktop + mobile, aria-current), guarded for tests
- TenderGrid rewritten as filterable tabs (Tous/Ouverts/En cours/Clôturés with counts); EmptyState preserved for zero-tender case
- Design tokens: Instrument Serif --font-display, container max-w-7xl, prefers-reduced-motion kill-switch, refined btn hover/focus states
- Landing rebuilt: gapless bento grid (border-divided ul), editorial hero 7xl serif, live DB stats strip, Reveal IO animations
- Final verification: tsc clean | 214/214 tests | build OK | all routes 200 | Playwright screenshots landing + flows
---
